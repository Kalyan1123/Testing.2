Thanks for sharing the error. Since the notebook runs fine on its own, the issue is with the "Delete Staging Table" activity in the pipeline, not the notebook.
Two quick things to confirm first:

What type of activity is "Delete Staging Table" — Script, Copy, Delete, or Notebook?
Is the staging table in a Lakehouse or a Warehouse?

What's likely happening:
If "Delete Staging Table" is a Script activity, that's the cause. Script activities in Fabric can't connect directly to a Lakehouse — they only support Warehouse, KQL, and SQL connections. The notebook works because it reaches the Lakehouse through Spark, which is a different path.
There's also a second catch: even routed through the Lakehouse SQL analytics endpoint, that endpoint is read-only. DROP and DELETE won't run against Lakehouse tables there. So reselecting or recreating the connection won't fix it.
Suggested fix:
Remove the Script activity and handle the cleanup inside a Notebook activity instead.
Drop the table:
pythonspark.sql("DROP TABLE IF EXISTS staging_table")
Or clear the rows only:
pythonspark.sql("DELETE FROM staging_table")
Use the bare table name for a Lakehouse. Add schema. only if your Lakehouse is schema-enabled (use the real schema name, not dbo).
If the table is actually in a Warehouse, the Script activity is fine — just repoint its connection from the Lakehouse to the Warehouse.
Please confirm the activity type and where the table lives so I can pin down the exact cause.

Notes for you:
Fixed vs image draft. Two things changed.
Code: dropped dbo. prefix. That's Warehouse syntax. Breaks on Lakehouse Spark. Bare name correct.







**Real problem:** Notebook activity's **Connection** field bound to a **Lakehouse** connection (via library variable). That field expects an identity (Service Principal / Workspace Identity / user), not a data connection. Lakehouse type invalid there → error.

**Why other pipeline works:** Delete Data activity accepts Lakehouse connection. Notebook activity doesn't. Same variable, different rules.

**Tell user to do (pick one):**
1. Clear the Connection field → notebook runs as user. Simplest.
2. Or set Connection to SPN/WI connection.

Lakehouse stays inside notebook, not in Connection field.

**Check with user:** After fix, does pipeline run clean? That's it. No other questions left.

**Message to paste:**

---

Found it. The Notebook activity's Connection field is set to a Lakehouse connection through your library variable. That field defines the identity the notebook runs as (Service Principal / Workspace Identity / user) — not a data source. A Lakehouse connection isn't valid there, hence "The datasource type Lakehouse is invalid."

It works in your other pipeline because Delete Data activities accept Lakehouse connections. Notebook activities don't.

Fix: clear the Connection field (notebook runs as the invoking user), or set it to a Service Principal / Workspace Identity connection. The Lakehouse is already attached inside the notebook — that's all it needs.

Try that and let me know if the pipeline runs clean.


Great news. Quick confirmation for the record: what change did you make — cleared the Connection field, or switched it to a Service Principal / Workspace Identity connection? Good to document the fix in case it comes up again.
If the staging table deletion worked as expected, we can consider this resolved.



If(
    IsBlank(ThisItem.'Related LCM 2'.'LCM Name'),
    "-",
    ThisItem.'Related LCM 2'.'LCM Name'
)

!IsBlank(ThisItem.'Related LCM 2'.'LCM Name')



If(
    IsBlank(ThisItem.'Related LCM 2'.Email),
    Blank(),
    IfError(
        If(
            Office365Users.UserPhotoMetadata(ThisItem.'Related LCM 2'.Email).HasPhoto,
            Office365Users.UserPhotoV2(ThisItem.'Related LCM 2'.Email),
            'User, person, man, human'
        ),
        'User, person, man, human'
    )
)
